pipeline {
  
  agent {
    label {
      label "built-in"
      customWorkspace /mnt/ass
    }
  }
  stages {
    stage (stage1) {
      steps {
        sudo "yum install git -y"
        sudo "ansible-playbook test.yaml
   
  
  
  
  
  
  
  }
}
